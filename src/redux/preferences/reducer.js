import {} from '../types';

const initial_state = {};

// preferences reducer
const reducer = (state = initial_state, action) => {
  switch (action.type) {
    default: return { ...state };
  }
};

export default reducer;
